import { FC } from "react";
import { CreateFieldLayoutContainer } from "./styles";

const CreateFieldLayout: FC = ({ children }) => {
  return <CreateFieldLayoutContainer>{children}</CreateFieldLayoutContainer>;
};

export default CreateFieldLayout;
